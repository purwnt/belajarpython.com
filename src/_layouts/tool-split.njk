<!DOCTYPE html>
<html lang="{{ lang if lang else 'id' }}" class="h-full">
  {% include "head.njk" %}
  <body class="h-full flex flex-col bg-slate-50 dark:bg-slate-900 overflow-hidden">
    {% include "navbar.njk" %}
    <main class="flex-1 flex flex-col h-full min-h-0 relative">
       {{ content | safe }}
    </main>
    {% include "progress-script.njk" %}
    {% include "analytics.njk" %}
  </body>
</html>
